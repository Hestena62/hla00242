<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DevBlog - .NET Core Simulation</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        /* Custom .NET Purple Color */
        .bg-dotnet { background-color: #512bd4; }
        .text-dotnet { color: #512bd4; }
        .border-dotnet { border-color: #512bd4; }
        .hover-bg-dotnet:hover { background-color: #4322b3; }
        
        /* Syntax highlighting mock */
        .code-keyword { color: #d73a49; }
        .code-class { color: #6f42c1; }
        .code-string { color: #032f62; }
        .code-comment { color: #6a737d; }
        
        /* Smooth transitions */
        .fade-in { animation: fadeIn 0.3s ease-in-out; }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    </style>
</head>
<body class="bg-gray-50 flex flex-col min-h-screen font-sans">

    <!-- Navigation Bar -->
    <nav class="bg-white border-b border-gray-200 shadow-sm sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex">
                    <div class="flex-shrink-0 flex items-center cursor-pointer" onclick="navigateTo('home')">
                        <!-- .NET Logo Mock -->
                        <div class="bg-dotnet text-white font-bold rounded-full w-8 h-8 flex items-center justify-center mr-2">
                            .NET
                        </div>
                        <span class="font-semibold text-xl text-gray-800 tracking-tight">DevBlog</span>
                    </div>
                    <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
                        <a href="javascript:navigateTo('home')" class="border-dotnet text-gray-900 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                            Home
                        </a>
                        <a href="#" class="border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                            About
                        </a>
                    </div>
                </div>
                <div class="flex items-center">
                    <span class="text-xs text-gray-400 mr-2 border border-gray-200 p-1 rounded">Debug Mode: On</span>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Layout Grid -->
    <main class="flex-grow max-w-7xl mx-auto py-10 px-4 sm:px-6 lg:px-8 w-full grid grid-cols-1 lg:grid-cols-3 gap-8">
        
        <!-- Left Column: Dynamic Application Content -->
        <div class="lg:col-span-2 space-y-6" id="app-root">
            <!-- Content injected via JavaScript -->
        </div>

        <!-- Right Column: Educational "Under the Hood" View -->
        <div class="lg:col-span-1">
            <div class="bg-gray-900 rounded-lg shadow-xl overflow-hidden sticky top-24">
                <div class="bg-gray-800 px-4 py-2 border-b border-gray-700 flex justify-between items-center">
                    <span class="text-gray-200 text-sm font-mono font-semibold">Server Logic (C#)</span>
                    <div class="flex space-x-1">
                        <div class="w-3 h-3 rounded-full bg-red-500"></div>
                        <div class="w-3 h-3 rounded-full bg-yellow-500"></div>
                        <div class="w-3 h-3 rounded-full bg-green-500"></div>
                    </div>
                </div>
                
                <!-- Tab Buttons -->
                <div class="flex border-b border-gray-700 bg-gray-800 overflow-x-auto">
                    <button onclick="showTab('controller')" id="btn-controller" class="px-3 py-2 text-xs font-mono text-white bg-gray-700 border-t-2 border-blue-400 flex-shrink-0">Controller.cs</button>
                    <button onclick="showTab('index')" id="btn-index" class="px-3 py-2 text-xs font-mono text-gray-400 hover:bg-gray-700 hover:text-white flex-shrink-0">Index.cshtml</button>
                    <button onclick="showTab('post')" id="btn-post" class="px-3 py-2 text-xs font-mono text-gray-400 hover:bg-gray-700 hover:text-white flex-shrink-0">Post.cshtml</button>
                    <button onclick="showTab('model')" id="btn-model" class="px-3 py-2 text-xs font-mono text-gray-400 hover:bg-gray-700 hover:text-white flex-shrink-0">Model.cs</button>
                </div>

                <!-- Code Content -->
                <div class="p-4 overflow-x-auto text-xs font-mono leading-5 h-96 overflow-y-auto bg-gray-900 text-gray-300">
                    
                    <!-- Controller Code -->
                    <div id="code-controller">
<pre><span class="code-keyword">using</span> Microsoft.AspNetCore.Mvc;
<span class="code-keyword">using</span> BlogApp.Models;

<span class="code-keyword">public class</span> <span class="code-class">BlogController</span> : <span class="code-class">Controller</span>
{
    <span class="code-keyword">private readonly</span> <span class="code-class">BlogService</span> _service;

    <span class="code-comment">// GET: /</span>
    <span class="code-keyword">public</span> <span class="code-class">IActionResult</span> Index()
    {
        <span class="code-comment">// Fetch all posts</span>
        <span class="code-keyword">var</span> posts = _service.GetAllPosts();
        <span class="code-keyword">return</span> View(posts);
    }

    <span class="code-comment">// GET: /Blog/Post/{id}</span>
    <span class="code-keyword">public</span> <span class="code-class">IActionResult</span> Post(<span class="code-keyword">int</span> id)
    {
        <span class="code-keyword">var</span> post = _service.GetPostById(id);
        
        <span class="code-keyword">if</span> (post == <span class="code-keyword">null</span>)
            <span class="code-keyword">return</span> NotFound();

        <span class="code-keyword">return</span> View(post);
    }
}</pre>
                    </div>

                    <!-- Index View Code -->
                    <div id="code-index" class="hidden">
<pre>@model <span class="code-class">List&lt;BlogPost&gt;</span>

<span class="code-comment">@{ ViewData["Title"] = "Home"; }</span>

&lt;h1&gt;Latest Posts&lt;/h1&gt;

@foreach (<span class="code-keyword">var</span> post <span class="code-keyword">in</span> Model)
{
    &lt;div class="card"&gt;
        &lt;h2&gt;@post.Title&lt;/h2&gt;
        &lt;p&gt;@post.Summary&lt;/p&gt;
        &lt;a asp-action="Post" asp-route-id="@post.Id"&gt;
            Read More
        &lt;/a&gt;
    &lt;/div&gt;
}</pre>
                    </div>

                    <!-- Post View Code -->
                    <div id="code-post" class="hidden">
<pre>@model <span class="code-class">BlogPost</span>

<span class="code-comment">@{ ViewData["Title"] = Model.Title; }</span>

&lt;article&gt;
  &lt;h1&gt;@Model.Title&lt;/h1&gt;
  &lt;p class="meta"&gt;
     By @Model.Author 
     on @Model.PublishDate
  &lt;/p&gt;
  
  &lt;div class="content"&gt;
     @Html.Raw(Model.Content)
  &lt;/div&gt;
  
  &lt;a href="/"&gt;Back to Home&lt;/a&gt;
&lt;/article&gt;</pre>
                    </div>

                    <!-- Model Code -->
                    <div id="code-model" class="hidden">
<pre><span class="code-keyword">namespace</span> BlogApp.Models;

<span class="code-keyword">public class</span> <span class="code-class">BlogPost</span>
{
    <span class="code-keyword">public int</span> Id { <span class="code-keyword">get</span>; <span class="code-keyword">set</span>; }
    <span class="code-keyword">public string</span> Title { <span class="code-keyword">get</span>; <span class="code-keyword">set</span>; }
    <span class="code-keyword">public string</span> Summary { <span class="code-keyword">get</span>; <span class="code-keyword">set</span>; }
    <span class="code-keyword">public string</span> Content { <span class="code-keyword">get</span>; <span class="code-keyword">set</span>; }
    <span class="code-keyword">public string</span> Author { <span class="code-keyword">get</span>; <span class="code-keyword">set</span>; }
    <span class="code-keyword">public</span> <span class="code-class">DateTime</span> PublishDate { <span class="code-keyword">get</span>; <span class="code-keyword">set</span>; }
}</pre>
                    </div>

                </div>
                <div class="bg-gray-800 px-4 py-2 text-xs text-gray-500 italic">
                    The panel above shows the C# code executing for the current view.
                </div>
            </div>
        </div>

    </main>

    <!-- Footer -->
    <footer class="bg-white border-t border-gray-200 mt-auto">
        <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
            <p class="text-center text-sm text-gray-500">
                &copy; 2025 .NET Foundation Simulation. All rights reserved.
            </p>
        </div>
    </footer>

    <script>
        // ==========================================
        //  MOCK DATABASE (Edit this to add posts!)
        // ==========================================
        const blogPosts = [
            {
                id: 1,
                title: "Exploring ASP.NET Core",
                summary: "ASP.NET Core is a cross-platform, high-performance framework for building modern cloud-based apps.",
                author: "Gemini AI",
                date: "Dec 8, 2025",
                category: "Technology",
                content: `
                    <p>ASP.NET Core is a cross-platform, high-performance, open-source framework for building modern, cloud-enabled, Internet-connected apps.</p>
                    <h3 class="text-lg font-bold mt-4 mb-2">Why choose .NET?</h3>
                    <ul class="list-disc pl-5 space-y-2 mb-4">
                        <li><strong>Performance:</strong> It is one of the fastest web frameworks available.</li>
                        <li><strong>Cross-Platform:</strong> Runs on Windows, macOS, and Linux.</li>
                        <li><strong>Unified:</strong> Use the same language (C#) for backend, logic, and even frontend (via Blazor).</li>
                    </ul>
                    <p>In this example, we are simulating a standard MVC (Model-View-Controller) architecture.</p>`
            },
            {
                id: 2,
                title: "Understanding Dependency Injection",
                summary: "Learn how C# handles dependencies to create loosely coupled, testable code.",
                author: "Sarah Dev",
                date: "Dec 5, 2025",
                category: "Programming",
                content: `
                    <p>Dependency Injection (DI) is a software design pattern that is a technique for achieving Inversion of Control (IoC) between classes and their dependencies.</p>
                    <p class="mt-4">In ASP.NET Core, DI is a first-class citizen. You can inject services directly into your controllers without manually creating instances.</p>
                    <div class="bg-gray-100 p-3 rounded mt-4 font-mono text-sm text-gray-800">
                        public BlogController(IBlogService service) { ... }
                    </div>`
            },
            {
                id: 3,
                title: "Entity Framework: Code First",
                summary: "A guide to designing your database using C# classes instead of SQL scripts.",
                author: "Mike Data",
                date: "Dec 1, 2025",
                category: "Database",
                content: `
                    <p>Entity Framework (EF) Core is a lightweight, extensible, open-source and cross-platform version of the popular Entity Framework data access technology.</p>
                    <p class="mt-4">With <strong>Code First</strong>, you define your model objects in C# code, and EF generates the database schema for you automatically.</p>`
            }
        ];

        // ==========================================
        //  APPLICATION LOGIC (Client-side Router)
        // ==========================================
        
        const appRoot = document.getElementById('app-root');

        // Simple router logic
        function navigateTo(view, id = null) {
            appRoot.innerHTML = ''; // Clear content
            appRoot.classList.remove('fade-in');
            void appRoot.offsetWidth; // Trigger reflow for animation
            appRoot.classList.add('fade-in');

            if (view === 'home') {
                renderHome();
                // Update Code Tab
                showTab('index'); 
                document.getElementById('btn-index').click(); // Auto-select tab
            } else if (view === 'post') {
                renderPost(id);
                // Update Code Tab
                showTab('post');
                document.getElementById('btn-post').click(); // Auto-select tab
            }
        }

        // Render the List (Home)
        function renderHome() {
            // Hero Section
            let html = `
                <div class="text-center pb-10">
                    <h1 class="text-4xl tracking-tight font-extrabold text-gray-900 sm:text-5xl md:text-6xl">
                        <span class="block xl:inline">Welcome to the</span>
                        <span class="block text-dotnet xl:inline">.NET DevBlog</span>
                    </h1>
                    <p class="mt-3 max-w-md mx-auto text-base text-gray-500 sm:text-lg md:mt-5 md:text-xl md:max-w-3xl">
                        A simulation of a server-side rendered application. Click a post below to navigate.
                    </p>
                </div>
                <div class="grid gap-5 max-w-lg mx-auto lg:grid-cols-2 lg:max-w-none">
            `;

            // Loop through data
            blogPosts.forEach(post => {
                html += `
                    <div class="flex flex-col rounded-lg shadow-lg overflow-hidden bg-white hover:shadow-xl transition-shadow cursor-pointer" onclick="navigateTo('post', ${post.id})">
                        <div class="flex-shrink-0 h-48 bg-gray-200 w-full flex items-center justify-center text-gray-400">
                             <!-- Placeholder Image Logic -->
                             <i class="fas fa-code text-4xl text-dotnet opacity-50"></i>
                        </div>
                        <div class="flex-1 bg-white p-6 flex flex-col justify-between">
                            <div class="flex-1">
                                <p class="text-sm font-medium text-dotnet">
                                    ${post.category}
                                </p>
                                <a href="#" class="block mt-2">
                                    <p class="text-xl font-semibold text-gray-900">${post.title}</p>
                                    <p class="mt-3 text-base text-gray-500">${post.summary}</p>
                                </a>
                            </div>
                            <div class="mt-6 flex items-center">
                                <div class="flex-shrink-0">
                                    <span class="sr-only">${post.author}</span>
                                    <div class="h-10 w-10 rounded-full bg-gray-300 flex items-center justify-center text-xs font-bold text-gray-600">${post.author.charAt(0)}</div>
                                </div>
                                <div class="ml-3">
                                    <p class="text-sm font-medium text-gray-900">${post.author}</p>
                                    <div class="flex space-x-1 text-sm text-gray-500">
                                        <time datetime="${post.date}">${post.date}</time>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                `;
            });

            html += `</div>`;
            appRoot.innerHTML = html;
        }

        // Render Single Post
        function renderPost(id) {
            const post = blogPosts.find(p => p.id === id);
            
            if (!post) {
                appRoot.innerHTML = `<div class="text-center py-20"><h2 class="text-2xl text-gray-600">404: Post Not Found</h2><button onclick="navigateTo('home')" class="text-dotnet mt-4 underline">Go Home</button></div>`;
                return;
            }

            appRoot.innerHTML = `
                <!-- Breadcrumb -->
                <nav class="flex text-sm text-gray-500 mb-4" aria-label="Breadcrumb">
                    <ol class="inline-flex items-center space-x-1 md:space-x-3">
                        <li class="inline-flex items-center">
                            <a href="javascript:navigateTo('home')" class="hover:text-gray-900">Home</a>
                        </li>
                        <li><i class="fas fa-chevron-right text-xs mx-2"></i></li>
                        <li aria-current="page">
                            <span class="text-gray-900 font-medium">${post.title}</span>
                        </li>
                    </ol>
                </nav>

                <article class="bg-white shadow overflow-hidden sm:rounded-lg">
                    <div class="px-4 py-5 sm:px-6 border-b border-gray-200 flex justify-between items-center">
                        <div>
                            <h1 class="text-3xl leading-8 font-bold text-gray-900">
                                ${post.title}
                            </h1>
                            <p class="mt-1 max-w-2xl text-sm text-gray-500">
                                Posted on <time>${post.date}</time> by <span class="font-medium text-dotnet">${post.author}</span>
                            </p>
                        </div>
                        <span class="inline-flex items-center px-3 py-0.5 rounded-full text-sm font-medium bg-purple-100 text-purple-800">
                            ${post.category}
                        </span>
                    </div>
                    
                    <div class="px-4 py-5 sm:p-6 text-gray-700 space-y-4 leading-relaxed">
                        ${post.content}
                        
                        <div class="bg-blue-50 border-l-4 border-blue-400 p-4 mt-8">
                            <div class="flex">
                                <div class="flex-shrink-0">
                                    <i class="fas fa-info-circle text-blue-400"></i>
                                </div>
                                <div class="ml-3">
                                    <p class="text-sm text-blue-700">
                                        You are viewing Post ID: <strong>${post.id}</strong>. The code to the right shows how this view is rendered.
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="bg-gray-50 px-4 py-4 sm:px-6 flex justify-between items-center">
                        <button onclick="navigateTo('home')" class="text-dotnet font-medium text-sm hover:underline">&larr; Back to List</button>
                        <div class="flex space-x-4 text-gray-400 text-sm">
                            <span><i class="far fa-eye mr-1"></i> Read only</span>
                        </div>
                    </div>
                </article>
            `;
        }

        // Tab Switching Logic for the Code Block
        function showTab(tabName) {
            // Hide all
            ['controller', 'index', 'post', 'model'].forEach(t => {
                document.getElementById('code-' + t).classList.add('hidden');
                document.getElementById('btn-' + t).className = "px-3 py-2 text-xs font-mono text-gray-400 hover:bg-gray-700 hover:text-white flex-shrink-0";
            });
            
            // Show selected
            document.getElementById('code-' + tabName).classList.remove('hidden');
            
            // Highlight Button
            document.getElementById('btn-' + tabName).className = "px-3 py-2 text-xs font-mono text-white bg-gray-700 border-t-2 border-blue-400 flex-shrink-0";
        }

        // Initial Load
        navigateTo('home');

    </script>
</body>
</html>